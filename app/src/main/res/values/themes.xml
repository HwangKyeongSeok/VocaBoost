<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VocaBoost" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary 색상 -->
        <item name="colorPrimary">@color/purple_500</item>
        <!-- Primary Variant 색상 -->
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <!-- Secondary 색상 -->
        <item name="colorSecondary">@color/purple_200</item>
    </style>

    <style name="Theme.VocaBoost" parent="Base.Theme.VocaBoost" />
    <style name="Theme.Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@android:color/white</item> <!-- 상태바 흰색 -->
        <item name="android:navigationBarColor">@android:color/white</item> <!-- 네비게이션 바 흰색 -->
    </style>
</resources>